package util;

public class EclipseTricks {

    /*
     * Java code-completion templates...
     *

"fors" (For over characters of a String)

for (int ${index} = 0; ${index} < ${s:var(java.lang.String)}.length(); ${index}++) {
        char ch = ${s}.charAt(${index});
        ${line_selection}${cursor}
}



     */
}
